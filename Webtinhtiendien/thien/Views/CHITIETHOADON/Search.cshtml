@model IEnumerable<namj.Models.ChiTietHoaDon>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Search</title>
    <style>
        table {
            border-collapse: collapse;
            width: 100%;
        }

        th, td {
            border: 1px solid black;
            padding: 6px;
            text-align: left;
        }

        th {
            background-color: deepskyblue;
        }

        tr:nth-child(even) {
            background-color: #aaa;
        }
    </style>
</head>
<body>
    @using (Html.BeginForm("Search", "CHITIETHOADON"))
    {
        <div class="form-group">
            <label for="name">Tìm kiếm theo:</label>
            @Html.RadioButton("searchString", "MaDonGia") Mã Đơn Giá
            @Html.RadioButton("searchString", "SoHoaDon") Số Hóa Đơn
            @Html.RadioButton("searchString", "SoLuongKW") Số Lượng KW
            @Html.TextBox("search")
        </div>

        <button type="submit" class="btn btn-primary">Tìm kiếm</button>
        <button type="button" onclick="location.href='@Url.Action("Index", "CHITIETHOADON")'">
            Trở lại trang chính
        </button>
        <button type="button" onclick="location.href='@Url.Action("Search", "CHITIETHOADON")'">
            Trở Về
        </button>
    }
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MaDonGia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SoHoaDon)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SoLuongKW)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.MaDonGia)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SoHoaDon)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SoLuongKW)
                </td>
                <td>
                    @Html.ActionLink("Sửa", "Edit", new { id1 = item.MaDonGia, id2 = item.SoHoaDon }) |
                    @Html.ActionLink("Chi Tiết", "Details", new { id1 = item.MaDonGia, id2 = item.SoHoaDon }) |
                    @Html.ActionLink("Xóa", "Delete", new { id1 = item.MaDonGia, id2 = item.SoHoaDon })
                </td>
            </tr>
        }

    </table>

</body>
</html>
